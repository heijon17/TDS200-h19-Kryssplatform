import { ArrayAveragePipe } from './array-average.pipe';

describe('ArrayAveragePipe', () => {
  it('create an instance', () => {
    const pipe = new ArrayAveragePipe();
    expect(pipe).toBeTruthy();
  });
});
